# Copy this file to `conf/local/credentials.yml` and fill in your
# local MongoDB credentials. The actual `credentials.yml` is git-ignored.

mongo_creds:
  # URI for local dev matches docker-compose.yml: `mongodb://localhost:27017`
  uri: "mongodb://localhost:27017"
  db: "commerce_signals"